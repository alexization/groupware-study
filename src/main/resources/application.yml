spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SIGW
    username: root
    password: Back0106!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true
logging:
  level:
    sql: debug
